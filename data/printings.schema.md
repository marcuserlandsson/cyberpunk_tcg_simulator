# `data/printings.json` schema

**Generated file — do not hand-edit.** Produced by `scripts/fetch-printings.ts`
from `https://api.netdeck.gg/api/cards/cyberpunk` (the same primary source
`data/cards.json` was transcribed from — see `data/transcription-report.md`).
Regenerate with:

```
npm run fetch:printings
```

Hand edits will be silently discarded the next time the script runs, and the
generated file will drift from whatever you edited without warning.

## Purpose

One row per physical printing of a card — base art, alt art, and per-set
reprints are each their own row. `data/cards.json` describes gameplay rules
per unique card; `data/printings.json` describes the physical objects a
collector can own of that card. Collection-tracking state (owned counts,
etc.) is layered on top of printings elsewhere in the app — never in
`cards.json` or `src/engine/types.ts`.

## Fields

Mirrors the `Printing` interface in `src/ui/printings.ts` (Task 1) exactly —
this file's rows are parsed and validated against that module's zod schema.

| Field | Type | Meaning |
|---|---|---|
| `key` | `string` | The app's stable printing id: `` `${setCode}/${collectorNumber}` ``. Globally unique. |
| `cardId` | `string` | FK into `data/cards.json` ids (the API's card slug). |
| `setCode` | `string` | The API's set code, e.g. `"welcometonightcitybeta"`. |
| `setName` | `string` | Human-readable set name, e.g. `"Welcome to Night City (Beta)"`. |
| `collectorNumber` | `string` | Verbatim from the API, **including the `β` prefix** on beta-set numbers (e.g. `"β025"`). Never reformatted or zero-padded beyond what the API returned. |
| `rarity` | `string` | Open string — the API's own rarity vocabulary (e.g. `"Common"`, `"Epic"`, `"Legendary"`). **Not an enum**: new values may appear in future sets without a schema change. |
| `finish` | `string \| null` | Open string or `null` — the API's finish/foil vocabulary. **Not an enum**, and nullable (many printings have no finish variant recorded). |
| `artist` | `string` | Credited artist. May be an empty string when the API has none on file — never `null`. |
| `sourcePrintingId` | `string` | The API's own printing UUID. Kept for provenance and future re-sync; not used as the app's key (`key` is). |

## Key format rule

`key` is always exactly `` `${setCode}/${collectorNumber}` `` — e.g.
`"welcometonightcitybeta/β025"`. This is enforced both by the fetch script
(refuses to write otherwise) and by `tests/data/printings.test.ts` (guards
the committed file in CI).

## Sort order

The file is written sorted by `(setCode, collectorNumber)` (lexicographic on
both) so that regenerating produces stable, minimal diffs — new rows are
easy to spot in review, and existing rows don't reorder just because a new
set was added.

## Provenance / latest run

- Generated by: `scripts/fetch-printings.ts`
- Source API: `https://api.netdeck.gg/api/cards/cyberpunk`
- Run date: 2026-09-04
- Row count: 426 printings
- Card coverage: 141/141 cards in `data/cards.json`, each with ≥1 printing
- Distinct sets: 12 (`arasakademodeck`, `boxtoppersbeta`, `boxtoppersretail`,
  `embracingpowerbetastarterdeck`, `embracingpowerretailstarterdeck`,
  `mercdemodeck`, `prereleasebeta`, `PRM01`, `theheistbetastarterdeck`,
  `theheistretailstarterdeck`, `welcometonightcitybeta`,
  `welcometonightcityretail`)

## Regeneration

Re-run `npm run fetch:printings` whenever new alt arts or reprints appear
upstream. The script only ever appends new rows for new printings — existing
`key` values are stable across re-runs (same `setCode`/`collectorNumber` in,
same key out), so previously saved collection data keyed by printing `key`
needs no migration after a regeneration.

Pass `--images` to additionally download each printing's art to
`data/images/printings/<key with '/' replaced by '__'>.webp` (gitignored via
the existing `data/images/` entry; not run as part of this commit — the app
falls back to drawn card faces when art is absent).
